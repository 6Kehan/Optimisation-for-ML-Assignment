Q1 优化算法实验与分析
考虑函数：f(x,y)=x2+100y2初始条件为 (x0​,y0​)=(2,2)，总迭代次数为 200 次。
(I) 多算法函数值收敛对比
应用以下四种优化算法，选择合理的基准参数，在同一张图表上绘制函数值 vs. 迭代次数的曲线：

Polyak step size（Polyak 步长）
RMSProp（参数：α=0.2,β=0.9）
Heavy Ball（参数：α=0.01,β=0.9）
Adam（参数：α=0.1,β1​=0.9,β2​=0.999）

(II) Heavy Ball 算法学习率敏感性分析
针对 Heavy Ball 算法，固定 β=0.9，逐渐增加 α 直到出现不稳定状态：

选取学习率：α=0.006,0.01,0.02
绘制x 坐标 vs. 迭代次数的曲线（针对上述三组 α 值）

(III) 等高线轨迹绘图与问题分析
1. 轨迹绘制
绘制 f(x,y) 的等高线图，并叠加以下算法的优化轨迹：

Heavy Ball（β=0.9,α=0.01）
RMSProp（β=0.9,α=0.2）
Adam（β1​=0.9,β2​=0.999,α=0.1）

2. 问题解释
简要回答以下问题：

为什么该函数会导致 “之字形”（zig-zag）行为？
动量（momentum）如何影响震荡？
自适应方法如何应对 x 和 y 方向上的不同曲率？
为什么病态条件（ill-conditioning）会影响 α 的稳定性极限？

考虑 Rosenbrock 函数（也被称为著名的 “香蕉函数”）：f(x,y)=(1−x)2+100(y−x2)2初始条件为 (x0​,y0​)=(−1.25,0.5)，运行 3000 次迭代。
(I) 多算法函数值收敛对比
应用以下四种优化算法，选择合理的基准参数，在同一张图表上绘制函数值 vs. 迭代次数的曲线：

Polyak 步长（限制 α≤0.1）
RMSProp（参数：α=0.01,β=0.9）
Heavy Ball（参数：α=2×10−4,β=0.9）
Adam（参数：α=0.05,β1​=0.9,β2​=0.999）

(II) Adam 算法学习率敏感性分析
针对 Adam 算法，固定 β1​=0.9,β2​=0.999，逐渐增加 α 直至出现不稳定状态：

选取学习率：α=0.02,0.05,0.12
绘制x 坐标 vs. 迭代次数的曲线（针对上述三组 α 值）

(III) 等高线轨迹绘图与问题分析
1. 轨迹绘制
绘制 Rosenbrock 函数的等高线图，并叠加以下算法的优化轨迹：

Heavy Ball（β=0.9,α=2×10−4）
RMSProp（β=0.9,α=0.01）
Adam（β1​=0.9,β2​=0.999,α=0.05）

2. 问题解释
简要回答以下问题：

为什么基础优化方法在 Rosenbrock 函数上收敛缓慢（该函数具有狭窄且弯曲的山谷结构）？
动量（momentum）如何改变优化过程中的震荡 / 过冲（overshoot）现象？
自适应缩放机制如何影响沿函数 “山谷” 方向的优化进展？
为什么学习率 α 的稳定性极限对函数的曲率具有敏感性？